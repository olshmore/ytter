# ECS / DNS (copy to infra.env for local overrides)
AWS_REGION=eu-west-2
AWS_ACCOUNT_ID=211125664612

ECS_CLUSTER_NAME=ytter
ECS_SERVICE_NAME=ytter-api
TASK_FAMILY=ytter-api

ROUTE53_ZONE_NAME=ytter.co.uk
API_HOST=api.ytter.co.uk

ECR_REPOSITORY=ytter

# Fargate sizing (api + redis sidecar)
FARGATE_CPU=1024
FARGATE_MEMORY=2048
DESIRED_COUNT=1

# Optional (discovered at bootstrap / load-state.sh):
# VPC_ID=
# SUBNET_IDS=
# RDS_SECURITY_GROUP_IDS=
# ALB_DNS_NAME=
# TARGET_GROUP_ARN=
# ECS_SECURITY_GROUP_ID=
# SECRET_ARN=
